# RASID Security Report — x.com

- **Tool**: `subfinder`
- **Status**: `SUCCESS`
- **Findings**: **519**
- **Generated**: 2026-05-14 20:44 UTC

## Findings

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-swedish-2017-03-07-opu78989rw.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ias.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb-atl.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain customer.payments-web.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain sw20160521s.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain atls.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain legalrequests.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain secmon.prod.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp-atl.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web-agent-tools.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain www.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain forum.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mail.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain loadtest-job.orchestrator.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain o.money-dev-onprem.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.money-dev.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools.orchestrator.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain unute.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pagead2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain syndication.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain k937.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain cdc-service.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain xpat-api.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-http-client.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-mobile.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools-client.payments-web.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.money-dev.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.money-dev.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain customer.payments-web.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain documents-client.payments-web.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ton.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ldapmaster.pdxs.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain envoy-standalone.money-dev-onprem.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.money-dev.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ldap.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain prod2.xmidm.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dm-proxy.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain realm-a.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain realm-cloud1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-http.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain syndication-o.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain auth.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain charitybadge.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain customer-client.payments-web.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain envoy-standalone.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain secure.paypal.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain support.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain artifactory.local.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain documents.payments-web.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-swedish-2017-04-12-xzjian-4off-787889789.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-swedish-2017-02-09-zfslk789897.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain business.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain https.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.prod.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain puppetserver.pdxs.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain x.darkghost.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.money-dev.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain txn-risk-api.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.money-dev-onprem.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain swedish-2017-03-07-opu78989rw.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ton-staging.atla.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp-pdx.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain transparency.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain xpat-api.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain biz.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain smoke-test-client.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain interest-cron-jobs.orchestrator.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain temp-mobile-api.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain se-20161027-9rw858.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain secmon.dev.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain wz.prod.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb-pdx.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain developers.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain documents.payments-web.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp-pdx.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-swedish-2017-02-08.fsf32490ll.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools.orchestrator.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.money-dev-onprem.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dfir-iris.prod.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain hashicorpvault.local.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain blog.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain careers.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.orchestrator.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain sdn.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain check-ocr.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.money-dev.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-agent-tools.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain smoke-test-client.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.money-dev.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain o.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-d106-41-3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain docs.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain local.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pic.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain documents-client.payments-web.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain se20160512.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain audubon.pdxv.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain sdn.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.money-dev.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 31.210.106.211.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web.money-dev-onprem.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain trust.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp.money-dev-onprem.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain interest-cron-jobs.orchestrator.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain smf1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain swedish-2017-02-13-iilli897897sf.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-admin.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pdxa.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain premium.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.money-dev.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain p.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain o.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain txn-risk-api.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.orchestrator.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-web.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain cdc-service.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb-client.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain tpop-api.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools.web.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain xpat-api.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-mobile.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain privacy.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain terafab.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain preprod2.xmidm.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain cpcalendars.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain gdpr.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain money-dev-onprem.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api.sandbox.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain puppetserver.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb-pdx.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools.web.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.money-dev.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mx3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain partners.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain video.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain preprod.xmidm.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain localhost.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain internal-orchestrator-grpc.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain o.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain audubon.local.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-se20161108-ki98547572.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grok.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain jf-o.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain unute33.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain se20161005-a9876.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain se20161031-zhi98875dd.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.prod.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ldapmaster.atls.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-green.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb-client.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-agent-tools.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web-agent-tools.money-dev-onprem.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dm-proxy.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.money-dev-onprem.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web.web.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain secmon.staging.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.money-dev.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain check-ocr.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ldapmaster.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain devcommunity.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain sdn.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ads.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain xpat-api.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain p.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain o.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain raid10.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ldap.atls.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain p.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain artifactory.pdxs.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain chat-ws.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools.orchestrator.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.money-dev.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain surfer.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain wz.dev.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain upload.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain aa.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain track.php-site_id157947aid209euidthttp3a2f2fwww.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.money-dev.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain tdapi.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain staging.terafab.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain documents.payments-web.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain cdn.syndication.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain launchpad.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain internal-orchestrator-grpc.money-dev-onprem.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web-documents.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.money-dev.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain internal-orchestrator-grpc.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain se20160614-2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain cpanel.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain money-dev.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.prod.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.staging.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.staging.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain o.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ads-api.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-agent-tools.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain probe-t.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain tweetdeck.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain d106-41-3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain help.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webmail.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain cards.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain documents-client.payments-web.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain envoy-standalone.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-web.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain swedish-2017-02-09-zfslk789897.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain apps.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-http.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp.onprem-corp.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain tdweb.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain legal.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-green.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain none.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain hashicorpvault.atlv.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain create.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain envoy-standalone.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-admin.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web-documents.money-dev-onprem.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ldapmaster.local.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-se20161029-r985ww.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.money-dev.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp-atl.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.orchestrator.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain customer-client.payments-web.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web-documents.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain wz.staging.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain gcore-lootproxy-stage.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain audit-logs.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools-client.payments-web.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dm-proxy.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain prod.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain engineering.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain developer.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.orchestrator.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain platform.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain www.realm-cloud1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain p.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain unute22.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain hashicorpvault.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-raid10.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain rollouts.argocd.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain p.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain watch.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.money-dev.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain td.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain gitlab.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web-agent-tools.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp-atl.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.orchestrator.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.money-dev.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-swedish-2017-02-06.fsfssfsdad4322456.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain interest-cron-jobs.orchestrator.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain p.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain audubon.pdxs.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain console.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain internal-api.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain tw-ton.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain sw20160520.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.money-dev.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain realm-b.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain se20161029-r985ww.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain shop.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain artifactory.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.dev.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ru1.30.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp-pdx.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain handles.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools.payments-web.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain cards-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain jf.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.money-dev.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain check-ocr.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-http-client.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain customer.payments-web.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain puppetserver.local.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-xcorp.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain o.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain swedish-2017-04-21-off4-fujian-788fs3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-admin.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-green.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain communitynotes.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain cpcontacts.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain sdn.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain puppetserver.atls.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain d106-b1-2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain swedish-2017-02-08.fsf32490ll.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain growth.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain loadtest-job.orchestrator.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb-client.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain sdn.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb-atl.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.dev.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ldap.pdxs.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web-documents.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.money-dev-onprem.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-web.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain o.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain preferencecenter.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain se20161108-ki98547572.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-swedish-2017-04-21-off4-fujian-788fs3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain cf.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain jf-t.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain c102-16-4.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb-pdx.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.orchestrator.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.lab.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain data.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain swedish-2017-04-12-xzjian-4off-787889789.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain caps.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.money-dev.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain swedish-2017-02-06.fsfssfsdad4322456.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain media.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain customer-client.payments-web.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain n3d24.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dfir-iris.staging.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-http-client.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain proofpoint.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain studio.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-se20161031-zhi98875dd.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain cdc-service.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain sdn.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain poet.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain www.shop.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.staging.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain analytics.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grow.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain identity.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.money-dev.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools-client.payments-web.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web-agent-tools.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-http.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-k937.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-sw20160521s.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dfir-iris.dev.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain artifactory.atls.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dnsmgr.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools.payments-web.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain prod.xmidm.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain gcore-lootproxy-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain marketing.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-stream.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain txn-risk-api.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain loadtest-job.orchestrator.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain grafana.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain verified.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webdisk.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain hashicorpvault.local.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.money-dev.money-dev1.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain vault.atls.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-n3d24.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-se20160512.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb.money-dev-onprem.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb.onprem-corp.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd-internal.dev.unute.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dnsmgr.atls.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dnsmgr.pdxs.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain o.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pro.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain krbsvc.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain community.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.money-dev.money-dev2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools.web.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain opensource.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain spylax.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain m.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain internal-orchestrator-grpc.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain orchestrator-grpc-mobile.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain webhooks.payments-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-swedish-2017-02-13-iilli897897sf.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain atla.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain agent-tools.payments-web.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain chat.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain karma.money-dev-onprem.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mx4.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain argocd.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain wr.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain payments-web.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain portal.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain spam.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain 220-se20160614-2.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain developer-staging.x.com.

**Evidence**:

```
source=hackertarget
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain admin.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain unute.money-dev-onprem.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api.money-staging.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain dnsmgr.local.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain about.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain api-lb-atl.money-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain pushgateway.money-dev.money-dev3.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain ldap.local.vault.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain games.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain mobile-api.money.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain lab.payments-dev.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain web-api.payments-prod.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---

### Subdomain Discovered

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: medium

Passive enumeration discovered subdomain live.x.com.

**Evidence**:

```
source=thc
```

**Recommendation**: Verify that the subdomain is intended to be public; orphan subdomains can lead to takeover.

---
