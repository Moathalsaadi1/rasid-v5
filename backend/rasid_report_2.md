# RASID Security Report — scanme.nmap.org

- **Tool**: `nmap`
- **Status**: `SUCCESS`
- **Findings**: **100**
- **Generated**: 2026-05-12 23:55 UTC

## Findings

### Open Service Detected

- **Severity**: `high` (critical)
- **Risk score**: 95
- **Confidence**: high

An open TCP service was detected on port 3389 (ms-wbt-server).

**Evidence**:

```
3389/tcp open ms-wbt-server
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `high` (critical)
- **Risk score**: 95
- **Confidence**: high

An open TCP service was detected on port 445 (microsoft-ds).

**Evidence**:

```
445/tcp open microsoft-ds
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `high` (critical)
- **Risk score**: 95
- **Confidence**: high

An open TCP service was detected on port 23 (telnet).

**Evidence**:

```
23/tcp open telnet
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `medium` (high)
- **Risk score**: 65
- **Confidence**: high

An open TCP service was detected on port 21 (ftp).

**Evidence**:

```
21/tcp open ftp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `medium` (high)
- **Risk score**: 65
- **Confidence**: high

An open TCP service was detected on port 5900 (vnc).

**Evidence**:

```
5900/tcp open vnc
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `medium` (high)
- **Risk score**: 65
- **Confidence**: high

An open TCP service was detected on port 3306 (mysql).

**Evidence**:

```
3306/tcp open mysql
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `medium` (high)
- **Risk score**: 65
- **Confidence**: high

An open TCP service was detected on port 5432 (postgresql).

**Evidence**:

```
5432/tcp open postgresql
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 53 (domain).

**Evidence**:

```
53/tcp open domain
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 79 (finger).

**Evidence**:

```
79/tcp open finger
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 80 (http).

**Evidence**:

```
80/tcp open http
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 81 (hosts2-ns).

**Evidence**:

```
81/tcp open hosts2-ns
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 88 (kerberos-sec).

**Evidence**:

```
88/tcp open kerberos-sec
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 106 (pop3pw).

**Evidence**:

```
106/tcp open pop3pw
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 110 (pop3).

**Evidence**:

```
110/tcp open pop3
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 111 (rpcbind).

**Evidence**:

```
111/tcp open rpcbind
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 113 (ident).

**Evidence**:

```
113/tcp open ident
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 119 (nntp).

**Evidence**:

```
119/tcp open nntp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 135 (msrpc).

**Evidence**:

```
135/tcp open msrpc
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 139 (netbios-ssn).

**Evidence**:

```
139/tcp open netbios-ssn
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 143 (imap).

**Evidence**:

```
143/tcp open imap
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 144 (news).

**Evidence**:

```
144/tcp open news
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 179 (bgp).

**Evidence**:

```
179/tcp open bgp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 199 (smux).

**Evidence**:

```
199/tcp open smux
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 389 (ldap).

**Evidence**:

```
389/tcp open ldap
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 427 (svrloc).

**Evidence**:

```
427/tcp open svrloc
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 443 (https).

**Evidence**:

```
443/tcp open https
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 444 (snpp).

**Evidence**:

```
444/tcp open snpp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 465 (smtps).

**Evidence**:

```
465/tcp open smtps
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 513 (login).

**Evidence**:

```
513/tcp open login
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 514 (shell).

**Evidence**:

```
514/tcp open shell
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 515 (printer).

**Evidence**:

```
515/tcp open printer
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 543 (klogin).

**Evidence**:

```
543/tcp open klogin
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 544 (kshell).

**Evidence**:

```
544/tcp open kshell
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 548 (afp).

**Evidence**:

```
548/tcp open afp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 554 (rtsp).

**Evidence**:

```
554/tcp open rtsp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 587 (submission).

**Evidence**:

```
587/tcp open submission
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 631 (ipp).

**Evidence**:

```
631/tcp open ipp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 646 (ldp).

**Evidence**:

```
646/tcp open ldp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 873 (rsync).

**Evidence**:

```
873/tcp open rsync
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 990 (ftps).

**Evidence**:

```
990/tcp open ftps
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 993 (imaps).

**Evidence**:

```
993/tcp open imaps
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 995 (pop3s).

**Evidence**:

```
995/tcp open pop3s
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1025 (NFS-or-IIS).

**Evidence**:

```
1025/tcp open NFS-or-IIS
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1026 (LSA-or-nterm).

**Evidence**:

```
1026/tcp open LSA-or-nterm
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1027 (IIS).

**Evidence**:

```
1027/tcp open IIS
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1028 (unknown).

**Evidence**:

```
1028/tcp open unknown
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1029 (ms-lsa).

**Evidence**:

```
1029/tcp open ms-lsa
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1110 (nfsd-status).

**Evidence**:

```
1110/tcp open nfsd-status
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1433 (ms-sql-s).

**Evidence**:

```
1433/tcp open ms-sql-s
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1720 (h323q931).

**Evidence**:

```
1720/tcp open h323q931
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1723 (pptp).

**Evidence**:

```
1723/tcp open pptp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1755 (wms).

**Evidence**:

```
1755/tcp open wms
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 1900 (upnp).

**Evidence**:

```
1900/tcp open upnp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 2000 (cisco-sccp).

**Evidence**:

```
2000/tcp open cisco-sccp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 2001 (dc).

**Evidence**:

```
2001/tcp open dc
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 2049 (nfs).

**Evidence**:

```
2049/tcp open nfs
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 2121 (ccproxy-ftp).

**Evidence**:

```
2121/tcp open ccproxy-ftp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 2717 (pn-requester).

**Evidence**:

```
2717/tcp open pn-requester
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 3000 (ppp).

**Evidence**:

```
3000/tcp open ppp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 3128 (squid-http).

**Evidence**:

```
3128/tcp open squid-http
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 3986 (mapper-ws_ethd).

**Evidence**:

```
3986/tcp open mapper-ws_ethd
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 4899 (radmin).

**Evidence**:

```
4899/tcp open radmin
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 5000 (upnp).

**Evidence**:

```
5000/tcp open upnp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 5009 (airport-admin).

**Evidence**:

```
5009/tcp open airport-admin
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 5051 (ida-agent).

**Evidence**:

```
5051/tcp open ida-agent
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 5060 (sip).

**Evidence**:

```
5060/tcp open sip
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 5101 (admdog).

**Evidence**:

```
5101/tcp open admdog
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 5190 (aol).

**Evidence**:

```
5190/tcp open aol
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 5357 (wsdapi).

**Evidence**:

```
5357/tcp open wsdapi
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 5631 (pcanywheredata).

**Evidence**:

```
5631/tcp open pcanywheredata
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 5666 (nrpe).

**Evidence**:

```
5666/tcp open nrpe
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 5800 (vnc-http).

**Evidence**:

```
5800/tcp open vnc-http
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 6000 (X11).

**Evidence**:

```
6000/tcp open X11
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 6001 (X11:1).

**Evidence**:

```
6001/tcp open X11:1
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 6646 (unknown).

**Evidence**:

```
6646/tcp open unknown
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 7070 (realserver).

**Evidence**:

```
7070/tcp open realserver
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 8000 (http-alt).

**Evidence**:

```
8000/tcp open http-alt
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 8008 (http).

**Evidence**:

```
8008/tcp open http
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 8009 (ajp13).

**Evidence**:

```
8009/tcp open ajp13
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 8080 (http-proxy).

**Evidence**:

```
8080/tcp open http-proxy
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 8081 (blackice-icecap).

**Evidence**:

```
8081/tcp open blackice-icecap
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 8443 (https-alt).

**Evidence**:

```
8443/tcp open https-alt
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 8888 (sun-answerbook).

**Evidence**:

```
8888/tcp open sun-answerbook
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 9100 (jetdirect).

**Evidence**:

```
9100/tcp open jetdirect
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 9999 (abyss).

**Evidence**:

```
9999/tcp open abyss
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 10000 (snet-sensor-mgmt).

**Evidence**:

```
10000/tcp open snet-sensor-mgmt
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 32768 (filenet-tms).

**Evidence**:

```
32768/tcp open filenet-tms
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 49152 (unknown).

**Evidence**:

```
49152/tcp open unknown
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 49153 (unknown).

**Evidence**:

```
49153/tcp open unknown
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 49154 (unknown).

**Evidence**:

```
49154/tcp open unknown
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 49155 (unknown).

**Evidence**:

```
49155/tcp open unknown
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 49156 (unknown).

**Evidence**:

```
49156/tcp open unknown
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 7 (echo).

**Evidence**:

```
7/tcp open echo
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 49157 (unknown).

**Evidence**:

```
49157/tcp open unknown
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 9 (discard).

**Evidence**:

```
9/tcp open discard
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 13 (daytime).

**Evidence**:

```
13/tcp open daytime
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 22 (ssh).

**Evidence**:

```
22/tcp open ssh
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 25 (smtp).

**Evidence**:

```
25/tcp open smtp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 26 (rsftp).

**Evidence**:

```
26/tcp open rsftp
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---

### Open Service Detected

- **Severity**: `info` (low)
- **Risk score**: 10
- **Confidence**: high

An open TCP service was detected on port 37 (time).

**Evidence**:

```
37/tcp open time
```

**Recommendation**: Review whether this service should be publicly exposed. Restrict access if not required.

---
